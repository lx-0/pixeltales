"""Agent layer — LLM-driven NPC reasoning entities.

One Agent per character: identity (`AGENTS.md`), capabilities (future
`SKILL.md` folders), configuration (`.character.yaml`). Channel-agnostic,
scene-agnostic, harness-agnostic. See `docs/architecture.md` for the
canonical 4-layer model.
"""
