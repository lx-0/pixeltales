// This package will contain shared types, interfaces, schemas, etc.

export type ExampleType = {
  id: string;
  name: string;
};

export const EXAMPLE_CONSTANT = "Hello from Contracts!";

// Add your shared types and constants here
